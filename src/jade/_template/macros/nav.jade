-
    var linkText = [
        "О конференции",
        //"Спикеры",
        "Расписание"
    ];
    var linkTextEn = [
        "",
        //"",
        ""
    ];
    var linkHref = [
        "#video",
        //"#speaker",
        "#program"
    ];


mixin btn(textLang)
    a(href= textLang[i], class="nav__link", title= textLang[i])
        span.nav__link-text!= textLang[i]

.nav
    a(href="javascript:void(0);", class="btn-mobile_close")
    -for(var i = 0; i < linkText.length; i++)
        if(!EN)
            +btn(linkText)
        else
            +btn(linkTextEn)
